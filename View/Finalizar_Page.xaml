<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Tamaleria_Miguelena.View.Finalizar_Page"
             xmlns:vm="clr-namespace:Tamaleria_Miguelena.ViewModel">
    <ContentPage.BindingContext>
        <vm:Login_ViewModel></vm:Login_ViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout>
            <Frame>
                <Button Command="{Binding CerrarCorte}" Text="Finalizar Turno" TextColor="White" FontAttributes="Bold" BackgroundColor="#8AD820"  CornerRadius="15" />
            </Frame>
            <Frame>
                <Button Text="Cerrar Tienda" TextColor="White" FontAttributes="Bold" BackgroundColor="Red"  CornerRadius="15" />
            </Frame>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>